class User:

	def user user(string firstName, string lastName, string email, span [] events)
		# Parameters: string firstName, string lastName, string email, span [] events
		# Returns: user
		# determine freeTimes from events?

	def printUser (user a)
		# Parameters: user
		# Returns: 
